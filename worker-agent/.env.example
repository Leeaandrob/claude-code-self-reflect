# Claude Self-Reflect Worker Agent Configuration
# Copy this file to .env and configure for your environment

# ============================================================================
# ADMIN API CONNECTION (Required for multi-machine setup)
# ============================================================================
# URL of the central Admin API where heartbeats are sent
ADMIN_API_URL=http://localhost:8000

# ============================================================================
# WORKER IDENTITY
# ============================================================================
# Unique identifier for this worker (defaults to hostname)
# WORKER_ID=my-workstation

# Heartbeat interval in seconds (default: 30)
# HEARTBEAT_INTERVAL=30

# Local agent API port (default: 8081)
# AGENT_PORT=8081

# ============================================================================
# QDRANT CONFIGURATION
# ============================================================================
# URL of the Qdrant instance (local or remote)
QDRANT_URL=http://localhost:6333

# ============================================================================
# EMBEDDING PROVIDER (Required - choose one)
# ============================================================================
# Option 1: Qwen/DashScope (2048 dimensions) - Recommended
# DASHSCOPE_API_KEY=your_dashscope_api_key

# Option 2: Voyage AI (1024 dimensions)
# VOYAGE_KEY=your_voyage_api_key

# Provider setting (cloud is the only supported mode)
EMBEDDING_PROVIDER=cloud

# ============================================================================
# PATHS
# ============================================================================
# Path to Claude conversation logs
CLAUDE_LOGS_PATH=${HOME}/.claude/projects

# Path to configuration files
CONFIG_PATH=${HOME}/.claude-self-reflect/config

# State file location
STATE_FILE=${HOME}/.claude-self-reflect/config/unified-state.json

# ============================================================================
# MEMORY DECAY (Optional)
# ============================================================================
# Enable time-based memory decay for search results
# ENABLE_MEMORY_DECAY=false
# DECAY_WEIGHT=0.3
# DECAY_SCALE_DAYS=90

# ============================================================================
# RESOURCE LIMITS
# ============================================================================
# Memory limit for safe-watcher container
# MEMORY_LIMIT_MB=1500

# Memory warning threshold
# MEMORY_WARNING_MB=750

# Maximum queue size for import
# MAX_QUEUE_SIZE=200
